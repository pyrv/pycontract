
TODO for Pyco

[ ] One cannot introduce a state after an event sequence.
[ ] If a state has no transitions it ends up with: def transition(self, event): match event:
[ ] __epsilon when a state occurs after an event sequence

--- ISSUES ---

[>] Wellformedness check
[ ] Command line version, CSV file processing, rows
[ ] Testing
[ ] Documentation

--- WAIT ---

CONSTRUCTS IN GRAMMAR KNOWN TO NOT BE HANDLED:

[ ] include
[ ] Python code in monitor
[ ] Statements

OTHER THINGS

[ ] Visualization, perhaps graphical input
[ ] Parameters to monitors, are other monitors parameters?, do we make instances of monitors
[ ] Reacting to errors .. callbacks?
[ ] Real time
[ ] invariants in monitor?
[ ] do construct on events
[ ] Statement language.
[ ] Other ways of providing extra output, labels
[ ] upto/accepton/rejecton
[ ] initializing a state with arguments
[ ] Annotate shorter versions, Perhaps with f-strings ...?
    monitor CorrectExecutionDaut {
      case Command(name = n?, number = x?): hot DoDispatch {
        veto DispatchFailure(name=n, number=x)
        case Dispatch(name=n, number=n): hot DoComplete{
          veto ExecutionFailure(name=n, number=x)
          case Complete(name=n, number=n): state DontComplete{
            veto Complete(name=n, number=n)
          }
        }
      }
    }

    monitor CorrectExecutionLogScope {
      case Command(name = n?, number = x?): [
        --- DoDispatch --------------------
        !DispatchFailure(name=n, number=x)
         Dispatch(name=n, number=n)
        --- DoComplete --------------------
        !ExecutionFailure(name=n, number=x)
         Complete(name=n, number=n)
        --- DontComplete ------------------
        !Complete(name=n, number=n)]
    }
[ ] State machines written as loosely coupled requirements

--- END ---

=====================
WELLFORMEDNESS CHECK:
=====================

[x] no duplicate event names (in grouped or ungrouped form)
[x] no duplicate argument names
[x] no duplicate monitor names
[x] each referenced event name (in patterns, vetoes, sequences, etc.) exists
[x] variables used in patterns are properly bound (e.g., n used only if n? appeared earlier in the scope)
[x] named argument matches a declared event field
[x] initial states must have no parameters
[x] state names unique within a monitor
[x] references to states (via TgtStateInstance) exist
[x] no undefined variables in name= expressions (unless bound by ?)
[x] no semi-uncreachable states (must be refereneced from somewhere)
[x] Variable shadowing	Warn if a new n? hides a parameter of the same name.
[x] positional args match the event arity
[x] target states should use the right parameter names
[x] no mixing named and positional arguments
[x] exists expressions

CAN WAIT:

[ ] type checking, ensure that variables bound by n? and reused later match the declared types of event fields.
[ ] refine the reachability analysis to follow transitive reachability (like a graph traversal)
[ ] include refers to existing monitors
[ ] monitor parameters different
[ ] type param matches events in transitions



